{% extends 'AdminBundle::layout.html.twig' %}

{% block datagrid %}

    <div class="row">
        <div class="col-xs-12">
            <div class="btn-group btn-group-md" role="group">
                <a href="{{ path('rs_meal_add') }}" class="btn btn-success">
                    <span class="glyphicon glyphicon-plus"></span> {{ 'rs_meal_add'|trans({},'menu') }}
                </a>
            </div>
        </div>
    </div>

    {{ grid(grid) }}

{% endblock %}
