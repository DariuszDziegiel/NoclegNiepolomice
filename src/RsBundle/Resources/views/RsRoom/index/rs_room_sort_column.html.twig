{# @var rsRoom \AdminBundle\Entity\rsRoom #}
{# @var maxSort int #}
<div class="btn-group" role="group">
    {% if rsRoom.sort != 0 %}
        <a href="{{ path('cms_article_sort', {'id': rsRoom.id, 'direction': 'up'}) }}" class="btn btn-default" data-toggle="tooltip" title="Przenieś wyżej">
            <span class="glyphicon glyphicon-arrow-up"></span>
        </a>
    {% endif %}
    {% if rsRoom.sort != maxSort %}
        <a href="{{ path('cms_article_sort', {'id': rsRoom.id, 'direction': 'down'}) }}" class="btn btn-default" data-toggle="tooltip" title="Przenieś niżej">
            <span class="glyphicon glyphicon-arrow-down"></span>
        </a>
    {% endif %}
    {% if rsRoom.sort != 0 %}
        <a href="{{ path('cms_article_sort', {'id': rsRoom.id, 'direction': 'first'}) }}" class="btn btn-default">
            <small>Przenieś na początek</small>
        </a>
    {% endif %}
    {% if rsRoom.sort != maxSort %}
        <a href="{{ path('cms_article_sort', {'id': rsRoom.id, 'direction': 'last'}) }}" class="btn btn-default">
            <small>Przenieś na koniec</small>
        </a>
    {% endif %}
</div>