{% extends 'AdminBundle::layout.html.twig' %}

{% block js %}
    {#<script src="{{ asset('bundles/rs/RsMeal/js/rs_meal.min.js') }}"></script> #}
{% endblock %}

{% block form %}

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.isActive, {'label': 'lbl.is_active'| trans}) }}
    <hr />

    {% if form.mainImageFile is defined %}
        {{ form_widget(form.mainImageFile) }}
        {% if rsPackage is defined and rsPackage.mainImage %}
            <div class="form-group">
                <div class="col-sm-2"></div>
                <div class="col-sm-10">
                    <br />
                    <br />
                    <br />
                    <img src="{{ asset('thumb.php') }}?src={{ asset(rsPackage.mainImageDir ~ rsPackage.mainImage) }}&w=300">
                </div>
            </div>
        {% endif %}
    {% endif %}
    {{ form_row(form.price) }}
    <!--
    <div class="row">
        <div class="col-xs-6">
            {% if form.detailsImageFile is defined %}
                {{ form_widget(form.detailsImageFile) }}
                {% if rsPackage is defined and rsPackage.detailsImage %}
                    <div class="form-group">
                        <div class="col-sm-2"></div>
                        <div class="col-sm-10">
                            <img src="{{ asset('thumb.php') }}?src={{ asset(rsPackage.detailsImageDir ~ rsPackage.detailsImage) }}&w=300">
                        </div>
                    </div>
                {% endif %}
            {% endif %}
        </div>
    </div>
   -->
    <hr />

    {{ form_widget(form.translations, {'label': 'lbl_tlumaczenia'| trans}) }}
    {% include '@Admin/_Common/partials/form_submit.html.twig' %}
    {{ form_end(form) }}

{% endblock %}