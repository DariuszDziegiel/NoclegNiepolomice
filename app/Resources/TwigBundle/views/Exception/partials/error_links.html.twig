{% if is_granted('ROLE_ADMIN')%}
    <a href="{{ path('admin_default') }}" class="btn btn-success">
        <span class="glyphicon glyphicon-home"></span>&nbsp;Powróć do strony głównej panelu / Return to the homepage
    </a>
{% else %}
    <a href="{{ path('homepage') }}" class="btn btn-success">
        <span class="glyphicon glyphicon-home"></span>&nbsp;Powróć do strony głównej  / Return to the homepage
    </a>
{% endif %}